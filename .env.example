# Port offset for parallel development environments
# Use this to run multiple instances of Baseplate simultaneously
# 
# Instance 0 (default): PORT_OFFSET=0
#   - Web: 4300
#   - Server: 4400  
#   - Dev Server: 4500
#
# Instance 1: PORT_OFFSET=1
#   - Web: 4301
#   - Server: 4401
#   - Dev Server: 4501
#
# Instance 2: PORT_OFFSET=2
#   - Web: 4302
#   - Server: 4402
#   - Dev Server: 4502

PORT_OFFSET=0

# Absolute port for `baseplate serve`, overriding PORT_OFFSET (optional)
# `baseplate serve` reads .env.local and .env from the directory it runs in.
# A plain PORT variable is ignored so generated app env files can't hijack the port.
# BASEPLATE_PORT=4400

# Path to local Baseplate VS Code extension for development (optional)
# Uncomment and set to your local extension path to mount it in the dev container
# BASEPLATE_DEV_EXTENSION_PATH=/path/to/your/local/baseplate-extension

# Path to local Baseplate documentation repository for development (optional)
# Uncomment and set to your local baseplate-docs repo path to mount it in the dev container
# BASEPLATE_DEV_DOCS_PATH=/path/to/your/local/baseplate-docs
