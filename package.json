{
  "name": "root",
  "private": true,
  "author": "Half Dome Labs LLC",
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "check-dependency-version-consistency": "^4.1.0",
    "nx": "15.7.2"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "watch": "nx run-many --target=watch --parallel=100 --output-style=stream",
    "lint": "nx affected --target=lint",
    "prettier:check": "nx affected --target=prettier:check",
    "prettier:format": "nx affected --target=prettier:format",
    "build": "nx run-many --target=build",
    "build:affected": "nx affected --target=build",
    "test": "nx affected --target=test",
    "release": "pnpm build && pnpm changeset publish",
    "dev:serve": "nx run project-builder-cli:dev:serve",
    "serve": "nx run project-builder-cli:start serve"
  },
  "volta": {
    "node": "18.16.0"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.1"
  },
  "packageManager": "pnpm@8.6.2"
}
