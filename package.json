{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "watch": "lerna run watch --parallel",
    "lint": "lerna run lint --no-bail"
  },
  "workspaces": [
    "packages/*"
  ]
}
