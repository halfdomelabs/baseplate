{
  "todo-with-better-auth/apps/admin tailwind-merge": "TODO: only libs/ui-shared imports tailwind-merge (via its cn helper); the admin app declares it without importing it. Drop it from the app generator when a shared component library is in use.",
  "todo-with-better-auth/apps/web react-icons": "TODO: only libs/ui-shared imports react-icons; the web app declares it without importing it. Drop it from the app generator when a shared component library is in use.",
  "todo-with-better-auth/apps/web sonner": "TODO: only libs/ui-shared imports sonner (the web app renders its re-exported Toaster); the web app declares it without importing it. Drop it from the app generator when a shared component library is in use.",
  "todo-with-better-auth/apps/web tailwind-merge": "TODO: only libs/ui-shared imports tailwind-merge (via its cn helper); the web app declares it without importing it. Drop it from the app generator when a shared component library is in use.",
  "todo-with-better-auth/libs/ui-shared @hookform/resolvers": "TODO: ui-shared declares @hookform/resolvers but only the apps import it; its own field components use react-hook-form directly. Drop it from the shared-library generator."
}
