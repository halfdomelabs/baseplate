Index: src/modules/todos/constants/file-categories.ts
===================================================================
--- src/modules/todos/constants/file-categories.ts
+++ src/modules/todos/constants/file-categories.ts
@@ -1,9 +1,14 @@
-import { createFileCategory, FileSize } from "../../storage/utils/create-file-category.js";
+import {
+  createFileCategory,
+  FileSize,
+} from '../../storage/utils/create-file-category.js';
 
-export const todoListCoverPhotoFileCategory = createFileCategory({adapter: 'uploads',
-authorize: {
-                        upload: ({ auth }) => auth.hasSomeRole(['user'])
-                      },
-maxFileSize: FileSize.MB(10),
-name: 'TODO_LIST_COVER_PHOTO',
-referencedByRelation: 'todoListCoverPhoto',})
\ No newline at end of file
+export const todoListCoverPhotoFileCategory = createFileCategory({
+  adapter: 'uploads',
+  authorize: {
+    upload: ({ auth }) => auth.hasSomeRole(['user']),
+  },
+  maxFileSize: FileSize.MB(10),
+  name: 'TODO_LIST_COVER_PHOTO',
+  referencedByRelations: ['todoListCoverPhoto'],
+});
