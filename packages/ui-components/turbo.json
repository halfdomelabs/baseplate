{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!README.md", "!*.stories.tsx"],
      "outputs": ["dist/**"]
    },
    "storybook:build": {
      "outputs": ["storybook-static/**"]
    }
  }
}
