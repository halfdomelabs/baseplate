import { Meta, Story } from '@storybook/blocks';

import * as TypographyStories from './Typography.stories.tsx';

<Meta of={TypographyStories} />

# Typography Guide

We use the [Geist](https://vercel.com/font) font for all our typography. It is a sans-serif font that is easy to read, influenced by the Inter font.

Below are the different typography styles we use in our design system with the CSS selector for the styles.

---

<Story of={TypographyStories.Headings} />
