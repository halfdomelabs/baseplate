// @ts-nocheck

import { Routes } from 'react-router-dom';

function TPL_ROUTES_NAME(): JSX.Element {
  TPL_ROUTE_HEADER;
  return (
    <Routes>
      <TPL_ROUTES />
    </Routes>
  );
}

export default TPL_ROUTES_NAME;
