fragment FileInput on FILE_SCHEMA {
  id
  name
  hostedUrl
}

mutation CreateUploadUrl($input: CreatePresignedUploadUrlInput!) {
  createPresignedUploadUrl(input: $input) {
    url
    fields {
      name
      value
    }
    method
    file {
      ...FileInput
    }
  }
}
