// Common auth generators that are shared across all auth implementations
// These will be populated as we extract common functionality from the implementations

// TODO: Extract common generators from local-auth, auth0, etc.
// Examples might include:
// - Role generation
// - Base auth types
// - Common auth utilities
