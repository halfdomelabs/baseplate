---
'@baseplate-dev/react-generators': patch
'@baseplate-dev/ui-components': patch
---

Form field descriptions and error messages are now announced by screen readers, radio and multi-switch groups get accessible names, every field accepts an `id` and `aria-describedby`, and the unused `FormItem`/`FormControl`/`useFormField` primitives have been removed from `@baseplate-dev/ui-components`.
