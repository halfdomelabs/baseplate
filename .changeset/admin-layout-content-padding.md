---
'@baseplate-dev/react-generators': patch
---

Add horizontal padding to the admin panel's main content area so page content no longer renders flush against the sidebar and window edges, and fix a Base UI console warning by marking the admin CRUD edit action button as non-native since it renders a link
