---
'@baseplate-dev/plugin-auth': patch
---

Updated generated code to use the renamed `services.email` app-runtime key (previously `services.emails`) in Better Auth's email dependency wiring and in the local-auth email verification and password reset services.
