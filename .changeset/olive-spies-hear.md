---
'@baseplate-dev/ui-components': patch
---

Adds Radio and Number field components, and form field controllers now share a single empty-value convention so clearing an optional field is saved instead of being silently ignored.
