---
'@baseplate-dev/project-builder-lib': patch
---

Added an `includeNotifications` flag to the web app definition, letting each web app in a project opt into the notifications plugin's frontend widget independently, mirroring the existing `includeUploadComponents` flag.
