---
'@baseplate-dev/plugin-auth': patch
---

Local auth now supports inviting a user by email to set up their account (reusing the password-reset link mechanism) via a "Send Invite" admin action, and each web app can independently disable self-service registration.
